server {
  listen 80;

  server_name flood.kanbaru.home;

  client_max_body_size 0;

  location / {
    include /etc/nginx/proxy.conf;

    set $protocol http;
    set $app_name flood;
    set $port 3000;
    proxy_pass $protocol://$app_name:$port;
  }
}
